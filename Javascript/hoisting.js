console.log(addone(5));
function addone(num) {
    return num +1
}


console.log(addTwo(5));
const addTwo = function(num){
    return num + 2
}

//in const we cant access add two before initialisation but in function we can access it before initialisation
